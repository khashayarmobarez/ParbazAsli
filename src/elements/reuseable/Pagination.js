import React from 'react';

const Pagination = ({totalCount, PageNumber, setPageNumber, totalPagesCount}) => {
    return (
        <div>
            
        </div>
    );
};

export default Pagination;